# Story 8.3: Test Coverage Gate & Release

**Status:** complete

## Story

≥80% coverage enforced in CI with working release process.

## Implementation

### Coverage Gate

- `jest.config.js` — Coverage thresholds set at 80% for branches, functions,
  lines, statements
- `ci.yml` — Runs tests with coverage, fails if below threshold
- `badges/coverage.svg` — Generated by `make-coverage-badge`
- Current coverage: **98.45%** (well above 80% threshold)

### Release Process

- `script/release` — Handles semver tagging and `v1` floating tag

### CI Verification

- `check-dist.yml` — Verifies `dist/index.js` matches compiled source

### Marketplace Metadata

- `action.yml` — Complete metadata: name, description, icon, color, branding

## File List

| File                       | Status   |
| -------------------------- | -------- |
| `jest.config.js`           | modified |
| `.github/workflows/ci.yml` | existing |
| `script/release`           | new      |
| `action.yml`               | existing |

## Requirements Traced

- NFR: ≥80% test coverage gate
- NFR: CI enforcement of coverage threshold
- NFR: Release process
